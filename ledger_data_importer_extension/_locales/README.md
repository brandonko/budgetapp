# Amazon localization catalog

These message catalogs belong to the upstream Amazon exporter and support its
popup and scraper. Chrome requires extension localization catalogs to remain in
the manifest-root `_locales` directory, so they cannot live under
`amazon_extension/` with the other Amazon-only files.

